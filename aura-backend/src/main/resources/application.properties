# Aura AI Backend Configuration
server.port=8080

# MongoDB dynamic configuration (uses env var in prod, falls back to localhost locally)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/aura}
